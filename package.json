{
  "name": "-----",
  "version": "1.0.0",
  "description": "تطبيق أندرويد تم إنشاؤه من ملفات HTML الخاصة بي.",
  "main": "index.js",
  "scripts": {
    "cordova:build": "cordova build android --debug",
    "cordova:add-platform": "cordova platform add android",
    "cap:add-android": "npm install @capacitor/android && npx cap add android",
    "cap:sync": "npx cap sync"
  },
  "dependencies": {
    "cordova-android": "^12.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  }
}